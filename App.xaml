<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="DNSManagerGUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DNSManagerGUI">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom Colors -->
            <Color x:Key="SidebarBackground">#FF181825</Color>
            <Color x:Key="CardBackground">#FF313244</Color>
            <Color x:Key="InputBackground">#FF45475A</Color>
            <Color x:Key="AccentColor">#FF89B4FA</Color>
            <Color x:Key="SuccessColor">#FFA6E3A1</Color>
            <Color x:Key="DangerColor">#FFF38BA8</Color>
            <Color x:Key="WarningColor">#FFF9E2AF</Color>
            <Color x:Key="TextPrimary">#FFCDD6F4</Color>
            <Color x:Key="TextSecondary">#FFA6ADC8</Color>
            <Color x:Key="TextMuted">#FF6C7086</Color>

            <!-- Navigation RadioButton Style — Catppuccin Mocha sidebar look -->
            <Style x:Key="NavigationViewRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#CDD6F4"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}"
                                    BorderThickness="0">
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootBorder.Background" Value="#1E1E2E"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootBorder.Background" Value="#313244"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="RootBorder.Background" Value="#313244"/>
                                                <Setter Target="RootBorder.BorderThickness" Value="2,0,0,0"/>
                                                <Setter Target="RootBorder.BorderBrush" Value="#89B4FA"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="RootBorder.Background" Value="#3B3B54"/>
                                                <Setter Target="RootBorder.BorderThickness" Value="2,0,0,0"/>
                                                <Setter Target="RootBorder.BorderBrush" Value="#89B4FA"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPressed">
                                            <VisualState.Setters>
                                                <Setter Target="RootBorder.Background" Value="#45475A"/>
                                                <Setter Target="RootBorder.BorderThickness" Value="2,0,0,0"/>
                                                <Setter Target="RootBorder.BorderBrush" Value="#89B4FA"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
